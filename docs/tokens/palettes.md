# Palettes

Base palettes are derived from Tailwind color scales and mapped to:

- `--theme-*` for brand selections
- `--base-*` for neutral selections

Use `data-theme-color` and `data-base-color` on `html` to switch palettes.
